services:
  - type: web
    name: thb-cod-proxy
    runtime: docker
    plan: starter
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: 5000
      - key: PLAYWRIGHT_BROWSERS_PATH
        value: /ms-playwright
